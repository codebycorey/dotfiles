#!/bin/bash
# Personal Scripts
#  ---------------------------------------------------------------------------
#
#  Description:  This file holds all my BASH scripts
#
#  ---------------------------------------------------------------------------
function command_exists() {
    type "$1" &> /dev/null ;
}
function moveMovie () {

    cp -R "$1" /data/store1/Movies

    chmod -R 755 /data/store1/Movies
}

function fixTrackPad() {
    if command_exists xinput ; then
        echo "Fixing Trackpad..."
        xinput --set-prop 11 "Synaptics Two-Finger Scrolling" 1 1
        xinput --set-prop 11 "Synaptics Scrolling Distance" -114 -114
    fi
}
fixTrackPad
